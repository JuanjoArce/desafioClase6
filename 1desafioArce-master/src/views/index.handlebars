<div>
    <h1>hola {{user.name}} que gusto verte</h1> 
    {{#if isAdmin}}
        {{#each food}}
            <div>
                <p>{{this.name}}</p>
                <p>{{this.price}}</p>
            </div>
        {{/each}}
    {{else}}
        <p> no tienes permiso para ver la lista de alimentos </p>
    {{/if}}
</div>
<script src="/1desafioArce-master/src/public/js/index.js"></script>